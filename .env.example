# Do NOT commit your .env file. Copy this to `.env` and populate your keys.
# Flutter dotenv will load these at runtime for local development.

# Google Maps API key for optional runtime checks (not used by Android manifest)
GOOGLE_MAPS_API_KEY=

# You can also add other env vars here (e.g., API_BASE_URL)
API_BASE_URL=
